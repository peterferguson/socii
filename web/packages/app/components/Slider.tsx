import { MotiView } from "moti"

// TODO: build 2nd slider from https://dribbble.com/shots/14976552-Slider-UI-Design
// ? to be used for choosing deposits and subcriptions on group page

const Slider = ({ maximumValue, value, onValueChange }) => {
  return <MotiView></MotiView>
}

export default Slider
